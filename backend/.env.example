# Configuración Local
PORT=4000
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rutazero
DB_USER=rutazero_admin
DB_PASSWORD=rutazero_2026
JWT_SECRET=tu_secreto_super_seguro_local

# Configuración Producción (Render)
# En Render, solo necesitas configurar DATABASE_URL (se hace auto vía render.yaml)
# DATABASE_URL=postgres://user:pass@host:port/db
